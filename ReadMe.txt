VE.yml is the Virtual Environment I used to build DataHandling, P1_InitialExploration, and P2_InitialExploration modules.

use "conda env create -f VE.yml" on your machine to clone it.

Change your WD to this folder before running any modules.
